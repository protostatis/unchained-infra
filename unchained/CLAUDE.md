# CLAUDE.md — Public Stub

This public repo intentionally omits the internal browser-operation playbook,
prompting heuristics, and extraction strategy notes used for private-core
development.

Use the public sources instead:

- `README.md` for the public product and repo overview
- `docs/architecture.md` for public architecture notes
- `uv run python cdp_tool.py --help` for packaged agent commands
- `uv run python tools/oss_guard/check_private_imports.py` for open-core checks

Detailed operator guidance lives only in the private workspace and the private
core repo.
